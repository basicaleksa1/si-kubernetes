apiVersion: apps/v1
kind: Deployment
metadata:
  name: bolnica-managemnt
  labels:
    app: bolnica-managemnt
spec:
  replicas: 3
  selector:
    matchLabels:
      app: bolnica-managemnt
  template:
    metadata:
      labels:
        app: bolnica-managemnt
    spec:
      containers:
      - name: bolnica-managemnt
        imagePullPolicy: Always
        image: ghcr.io/raf-si-2021/bolnica-managemnt-service:latest
        resources:
          limits:
            cpu: "1"
            memory: "1Gi"
        env: # napraviti env vaijable da bi mogli da lepo koristimo servise
          - name: "spring.datasource.url"
            value: "jdbc:mysql://management-db:3306/management?serverTimezone=UTC"
          - name: "spring.datasource.username"
            value: root
          - name: "spring.datasource.password"
            valueFrom: 
                secretKeyRef: 
                  name: bolnica-secret
                  key: mysql-root-password
                  optional: false
          - name: "spring.jpa.generate-ddl"
            value: "true"
          - name: "spring.jpa.hibernate.ddl-auto"
            value: "update"
        ports:
        - containerPort: 8082
---
apiVersion: v1
kind: Service
metadata:
  name: bolnica-managemnt
spec:
  selector:
    app: bolnica-managemnt
  ports: 
    - protocol: TCP
      port: 8082
      targetPort: 8082

# u ingresu servicePort i u servisu port treba da se poklapaju